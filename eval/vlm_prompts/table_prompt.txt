请从这张表格图片中提取完整的表格内容。输出严格的 JSON 格式，不要其他文字。

要求：
1. 识别所有列标题（包括多行表头的合并）
2. 逐行逐单元格提取所有内容
3. 如果单元格内是化学分子结构图（非文字），请尝试写出其 SMILES 表示
4. 保持原始数据不做修改（包括上下标标记如 [a], [b] 等脚注标记）

输出格式：
{
  "columns": ["列1", "列2", ...],
  "rows": [
    ["单元格1", "单元格2", ...],
    ...
  ]
}

注意：
- 空单元格用 "" 表示
- 数值保持原样（如 "70 (82)" 不要拆分）
- 分子结构图写 SMILES（如无法识别写 "[structure]"）
